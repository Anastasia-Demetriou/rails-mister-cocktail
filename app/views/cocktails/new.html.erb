


  <%= simple_form_for(@cocktails) do |f| %>

  <%= f.error_notification %>
  <%= f.error_notification message: f.object.errors[:base].to_sentence if f.object.errors[:base].present? %>

  <%= f.input :name %>
  <%= f.button :submit %>

   <%= link_to cocktail.name, cocktail_path(@cocktails) %>


  <% end %>
